# V5.1 GSE160763 preparation report

- Version: 5.1.0
- Profile: GSE160763 mouse cortex TBI/PLX5622
- Prepared for Windows R 4.5.3
- Core smoke config: log normalization, RPCA, advanced modules disabled
- Second config: SCTransform v2/RPCA plus formal DE evaluability gate

## Important scientific limitation

There are two pooled scRNA-seq libraries per condition. Each library pools three mice. The pool/library is the sequencing-level biological replicate, so the profile deliberately retains `minimum_replicates_per_condition: 3`. The second run should not report formal DE as evaluable.

## Validation status

Python/YAML/ZIP/static structure is checked in the build container. R runtime, numerical outputs, package compatibility and figure appearance remain to be tested locally.

- Config schema now permits disabled advanced modules to specify only `enabled: false`; backend fields remain required when enabled.
