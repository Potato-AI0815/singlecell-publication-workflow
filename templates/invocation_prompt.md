Use `$single-cell-publication-workflow` version 5 and its bundled R engine. Do not regenerate numbered scripts or write ad hoc plotting code when a template exists. Read `SKILL.md`, the selected recipe and `config.yml`; validate paths, metadata and module prerequisites; run `python tests/static_validate.py <skill-root>` and `python tests/validate_config.py <config>`, then run `Rscript run_all.R <config>`. Use biological samples as replicates, raw-count pseudobulk for formal DE and integrated reductions only for alignment/clustering/visualization. Run each advanced module only when its explicit gate passes. Export independent single figures only; never create a combined figure, montage or contact sheet. Each figure must include PDF, optional SVG, 600-dpi TIFF/PNG, proof PNG, plot RDS, parameters, Source Data and technical QA. Never hand-draw a trajectory arrow, force an annotation, infer DNA-confirmed CNA from RNA, turn drug scores into treatment advice, or describe virtual-knockout distance as signed expression change. For virtual knockout, predeclare target, coherent cell subset and baseline state; use sample-stratified outer subsampling and cross-sample consensus; retain its exploratory, unsigned interpretation. Report `NOT_RUN` or `NOT_EVALUABLE` rather than inventing a result, and do not declare completion while required QA is `FAIL`.
